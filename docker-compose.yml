services:
  cc-pet:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - cc-pet-data:/data
      - ./cc-pet.docker.config.json:/data/cc-pet.config.json
    restart: unless-stopped

volumes:
  cc-pet-data:
